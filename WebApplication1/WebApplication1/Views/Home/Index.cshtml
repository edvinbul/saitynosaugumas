@model HomeViewModel
<h1>Top cars</h1>
<div class="row mt-0 bg-opacity-50 mb-2">
    @{
        foreach (Car car in Model.favCars)
        {
            @Html.Partial("AllCars", car)
        }


    }

</div>

